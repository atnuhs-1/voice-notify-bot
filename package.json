{
  "name": "voice-notify-bot",
  "version": "1.0.0",
  "description": "This project was bootstrapped with Fastify-CLI.",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "test": "tap test/**/*.test.js",
    "build": "tsc",
    "start": "node dist/server.js",
    "start:js": "node server.js",
    "dev": "NODE_ENV=development nodemon --exec ts-node server.ts",
    "dev:js": "NODE_ENV=development nodemon server.js",
    "deploy-commands": "ts-node deploy-commands.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fastify/autoload": "^5.0.0",
    "@fastify/sensible": "^5.0.0",
    "@libsql/client": "^0.4.0",
    "discord.js": "^14.14.1",
    "dotenv": "^16.3.0",
    "fastify": "^4.0.0",
    "fastify-cli": "^6.0.0",
    "pino-pretty": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/tap": "^15.0.0",
    "nodemon": "^3.0.0",
    "tap": "^18.0.0",
    "ts-node": "^10.9.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.0.0"
  }
}